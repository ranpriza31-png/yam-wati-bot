services:
- type: web
  name: yam-wati-bot
  env: node
  plan: starter
  buildCommand: npm install
  startCommand: node index.js
  disk:
    name: auth-data
    mountPath: /data
    sizeGB: 1
  envVars:
  - key: ANTHROPIC_API_KEY
    sync: false
